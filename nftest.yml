---
global:
  temp_dir: ./test/work
  nf_config: test/global.config
  remove_temp: false
  clean_logs: true

cases:
  - name: test-integration
    message: Integration test
    nf_script: main.nf
    nf_config: test/test-integration/test.config
    skip: false
    verbose: true
